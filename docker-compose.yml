version: '3.9'
services:
  tankflix:
    image: ghcr.io/plan3t/tankflix:latest
    build: .
    container_name: tankflix
    ports:
      - "8111:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
